# Glim POC — 작업 원칙

## 한글 텍스트 개행 원칙 (필수)
- 모든 한글 텍스트(튜터 대사, 학습자 발화, 카드, 칩, 타이틀 등)는 **단어(어절) 중간에서 개행 금지**.
- 반드시 `word-break: keep-all` (+ 필요시 `overflow-wrap: break-word`)을 텍스트 컨테이너에 적용할 것.
- 새 텍스트 요소를 추가할 때마다 이 속성이 적용되어 있는지 확인.
